package com.hospital.backend.config;
// File intentionally cleared to prevent conflicting CORS setup with SecurityConfig.java.
// ALL CORS configuration should be handled strictly in SecurityConfig.java through the corsConfigurationSource Bean.